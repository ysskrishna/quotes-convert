# Interactive Playground

This is a simple playground stub where we will later add an interactive demo to try `quotes_convert` in the browser.

For now, you can run examples locally:

```bash
python -c "from quotes_convert import single_quotes; print(single_quotes('{\"a\": \"b\"}'))"
```

